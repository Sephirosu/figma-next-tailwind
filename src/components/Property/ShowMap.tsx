import Showmap from "@icons/showmap.svg";

const ShowMap = () => {
  return (
    <div className="fixed bottom-[128px] left-1/2 transform -translate-x-1/2 cursor-pointer">
      <Showmap className="bottom-10 " />
    </div>
  );
};

export default ShowMap;
